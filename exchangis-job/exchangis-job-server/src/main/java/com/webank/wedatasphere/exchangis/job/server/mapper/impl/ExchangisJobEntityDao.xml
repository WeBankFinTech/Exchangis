<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.webank.wedatasphere.exchangis.job.server.dao.ExchangisJobEntityDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.webank.wedatasphere.exchangis.job.domain.ExchangisJobEntity">
        <result column="id" property="id"/>
        <result column="name" property="jobName"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="engine_type" property="engineType"/>
        <result column="job_labels" property="jobLabel"/>
        <result column="create_user" property="createUser"/>
        <result column="job_content" property="jobContent"/>
        <result column="execute_user" property="executeUser"/>
        <result column="job_params" property="jobParams"/>
        <result column="project_id" property="projectId"/>
        <result column="source" property="source"/>
        <result column="modify_user" property="modifyUser"/>
    </resultMap>

    <sql id="TableName">
        exchangis_job_entity
    </sql>

    <!-- 通用查询结果列 -->
    <sql id="BaseColumnList">
        id,
        name, create_time, engine_type, job_labels, create_user, job_content, execute_user, job_params, project_id, source, modify_user
    </sql>

    <insert id="addJobEntity" useGeneratedKeys="true" keyProperty="id" parameterType="com.webank.wedatasphere.exchangis.job.domain.ExchangisJobEntity">
        insert into
        <include refid="TableName"/>
        (id, name, create_time, last_update_time, engine_type, job_labels, create_user, job_content, execute_user, job_params, project_id, source, modify_user)
        values(
        #{id},
        #{name},
        #{createTime},
        #{lastUpdateTime},
        #{engineType},
        #{jobLabel},
        #{createUser},
        #{jobContent},
        #{executeUser},
        #{jobParams},
        #{projectId},
        #{source},
        #{modifyUser}
        );
    </insert>

    <delete id="deleteJobEntity">
        delete * from
        <include refid="TableName"/>
        where id = #{jobId}
    </delete>

    <update id="upgradeJobEntity">
        UPDATE <include refid="TableName"/>
        SET
        id = #{id},
        name = #{name},
        create_time = #{createTime},
        last_update_time = #{lastUpdateTime},
        engine_type = #{engineType},
        job_labels = #{jobLabel},
        create_user = #{createUser},
        job_content = #{jobContent},
        execute_user = #{executeUser},
        job_params = #{jobParams},
        project_id = #{projectId},
        source = #{source},
        modify_user = #{modifyUser}
    </update>

    <select id="getJobEntity" parameterType="java.lang.String" resultMap ="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName" />
        where id = #{jobId}
    </select>

    <select id="getJobEntityByFactor" parameterType="java.lang.String" resultMap ="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName" />
        where project_id = #{projectId}
        <if test="name != null and name = ''">
            and name like '%#{name}%'
        </if>
    </select>

    <select id="getJobEntityByName" parameterType="java.lang.String" resultMap ="BaseResultMap">
        select
        <include refid="BaseColumnList"/>
        from
        <include refid="TableName" />
        where
        <if test="name != null and name = ''">
            and name like '%#{name}%'
        </if>
    </select>

</mapper>
